[package]
name = "sidereon_nif"
version = "0.11.1"
authors = []
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustler = "0.37.3"
chrono = "0.4"
serde_json = "1"
sidereon-core = { path = "/Users/neil/xuku/sidereon/crates/sidereon-core", version = "0.11.1" }
sidereon = { path = "/Users/neil/xuku/sidereon/crates/sidereon", version = "0.11.1" }
trust-region-least-squares = { path = "/Users/neil/xuku/sidereon/crates/trust-region-least-squares", version = "0.9.1" }
# Same major.minor as sidereon-core so the shared `DMatrix`/`DVector` types
# unify across crates for the covariance/least-squares marshalling.
nalgebra = "0.33"
